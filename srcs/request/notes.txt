Add parsing of ressources to create Content-language header?
Add parsing of ressources to check if they match Accept-language header?

Create header value parsing to separate/sort different values

No Host header or more than one Host == BAD REQUEST
Check if host:port does not match server config?

Nothing to do with Referer header?

Retry-After ? Anything more to do than sending default value?

Same for Server ?

Transfer-Encoding ? => no encoding == no problem ?

User-Agent => useless ?

What to do in case of unknown/un-implemented header?
	- Ignore
		OR
	- Bad Request ?
